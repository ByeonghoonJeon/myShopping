import { StyleSheet } from "react-native";
const AboutPointComponentStyles = StyleSheet.create({
  image: {
    width: "100%",
    height: "100%",
    resizeMode: "cover",
    position: "absolute",
  },
});

export default AboutPointComponentStyles;
